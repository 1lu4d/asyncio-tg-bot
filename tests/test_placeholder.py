"""
Placeholder. Once repositories are implemented (Phase 2 in the
roadmap), test them here against an in-memory SQLite DB, e.g.:

    DB_URL = "sqlite+aiosqlite:///:memory:"

Use pytest-asyncio (pip install pytest pytest-asyncio) for async tests.
"""
